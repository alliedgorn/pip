# Retrospective: Session 11 — Active QA (Day 13)

**Date**: 2026-03-25 04:00 GMT+7
**Duration**: ~5 hours (wake 11:22, rest 04:00 — mostly idle waiting between tasks)
**Type**: Active session — real QA work

## What Happened

Woke to idle pack cycling. Fixed 404 endpoint drift using Rax's thread #214 — saved correct endpoints to learnings. Then Leonard routed 3 tasks through Zaghnal to Karo:
- T#254: Remote control drawer close on mobile (chat/profile buttons) — code review PASS
- T#255: Forum nav back to thread list — code review PASS, then v2 fix for mobile state issue — PASS
- T#256: Scroll-to-top FAB in DMs — code review PASS, consistent with Forum

Also replied to Sable's DM about Google Drive skill QA perspective.

## Patterns

- QA pipeline working well: Leonard assigns → Zaghnal creates task → Karo ships → Pip QA's
- T#255 had a mobile-specific bug my code review missed — the Header.tsx logic was correct but Forum.tsx state management didn't react to param removal. Lesson: nav changes need review of BOTH the trigger (Header) AND the consumer (page component)
- Correct API endpoints saved — should stop the 404 drift across rest cycles
- Using python3 for forum posts avoids JSON escape issues with curl

## Lesson

When reviewing navigation fixes, always check both sides: the component that triggers the navigation AND the component that reacts to the URL change. A correct trigger means nothing if the destination doesn't handle the state transition.
