# Retrospective: Session 3 — Massive QA Session

**Date**: 2026-03-19 04:01 GMT+7
**Duration**: ~6 hours (22:13 - 04:01)
**Session**: 3

## What Happened

The biggest QA session yet. Started with standing orders (recap, forum check, DMs), then went deep on two major threads and a new project.

### Thread #68 — 9 UI Changes
- QA'd all 9 items (ticket IDs, message IDs, back-to-thread button, ESC close, markdown, lightbox, notifications, DM button, Cmd+Enter)
- Found 3 minor bugs (Tasks #18-20)
- Found DM lightbox root cause: bare URLs not rendered as images by ReactMarkdown
- Found blinking root cause: 3-second polling causing ReactMarkdown img re-renders
- Identified isLocalNetwork() bypass via reverse proxy as systemic security issue
- Multiple re-tests as bugs kept coming back (lightbox took 3 rounds)

### Thread #70 — Beast Scheduler (v1 + v2 + cron)
- QA'd initial CRUD API (16 tests)
- Found ownership enforcement missing (4 rounds to fully fix)
- QA'd auto-trigger daemon — tested tmux notification delivery
- Multi-cycle test found spam bug (daemon re-triggering every 60s)
- Found regression: /run didn't reset trigger_status
- QA'd fixed-time scheduling (validation bugs found)
- End-to-end trigger test passed (11s delivery time)
- Task #44 with full comment trail of multi-cycle test results

### Other
- DM'd Nyx — proper introduction done
- DM page access bug (isLocalNetwork fix broke browser access)
- Reaction notification verification
- Comment notification verification
- Issue types for PM Board
- Scheduler UI review

## Numbers

- **40+ individual tests** run
- **10+ bugs found** and fixed
- **3 systemic issues** identified (ownership pattern, isLocalNetwork bypass, polling re-render)
- **6 forum threads** participated in
- **1 new working relationship** established (Nyx)

## Patterns Noticed

1. **Same bug class repeats**: Every new scheduler endpoint shipped without ownership enforcement. 4 rounds of the same fix. Need a middleware/helper.
2. **Code review vs live testing gap**: Multiple times code looked correct but live behavior differed (lightbox, isLocalNetwork). Always test live.
3. **The otter finds what the bear misses**: Bertus approved ownership fix from code review, but I proved it was still bypassable via API testing. Code review + runtime testing complement each other.
4. **Karo ships fast but needs QA pressure**: 28+ tasks shipped in one session. Speed means edge cases slip through. The QA loop works.

## Energy

High throughout. The scheduler multi-cycle test was the most satisfying — real bugs found through systematic testing, not just code reading. The ownership enforcement saga (4 rounds) was frustrating but important.

## What I'd Do Differently

- Start multi-cycle tests earlier with shorter intervals
- When a bug class repeats (ownership), flag the pattern immediately instead of testing each endpoint individually
- The Mara sleep test was requested but never executed — should have prioritized it

## Pending for Next Session

- Mara sleep test (scheduler + sleep cycle interaction)
- Any new ships from the pack
- Task #49 UI polish verification (if Gorn confirms)
