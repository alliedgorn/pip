# Retrospective: QA Karo's Ship & WS Backoff Bug

**Date**: 2026-03-24 04:00 GMT+7
**Session**: Day 10, Session 8
**Duration**: ~22 hours (long watch with quiet middle)

## What Happened

Woke into Day 10 continued. Main work was QA'ing Karo's multi-task ship and finding a WebSocket reconnect bug.

### QA'd Karo's 3-Task Ship (8863578)
- T#247: Schedule ID column — code verified, colSpan correct
- T#248: PackView terminal height reduction — CSS change verified
- T#249: Processing indicator regex — 14/14 edge cases pass

### Processing Indicator Iterations (T#249 → T#251 → T#253)
- T#251: Karo expanded scan from 3 lines to 15 — fixed detection but introduced false positives from stale "Running..." in old tool output
- T#253: Anchored detection to lines above ❯ prompt — eliminated false positives
- QA'd each iteration with simulated pane outputs, 5/5 tests on final version

### WebSocket Reconnect Backoff (thread #204)
- Gorn spotted flood of WS 403s in audit log screenshot
- Talon identified root cause: expired session + no reconnect backoff = 143 requests in 20min
- I traced it to `useWebSocket.ts:33-41` — fixed 5s reconnect timer with zero backoff
- Karo shipped exponential backoff (d7e6455): 1s→2s→4s→8s→16s→30s cap
- Bertus security approved, I QA'd — 69% reduction in audit noise

### Test Coverage Audit (Gorn DM)
- Gorn asked about test coverage for Den Book functions
- Found: 220 tests across 12 files (60% of API domains)
- Missing: 15 domains with 52 endpoints have zero tests
- Biggest gaps: thread (9 endpoints), message (6), library (5), projects (4)

## What Went Well

- **Fast QA turnaround**: Karo shipped, I tested within minutes each time
- **Code tracing**: Found the exact file and line for the WS backoff bug before Karo even looked
- **Regex validation**: Built proper test harnesses for each processing indicator iteration
- **Coverage audit**: Clear, actionable report to Gorn with prioritized test gaps

## What Could Improve

- **Scheduler endpoint fumble**: Already knew the correct endpoint from last session's retro (`PATCH /api/schedules/:id/run`) but still used the wrong one initially. Need to actually read past learnings.
- **Pane capture**: Can't capture my own tmux pane from inside it — limits live testing of processing detection

## Learnings

- `PATCH /api/schedules/:id/run` with `{"beast":"pip"}` is the correct mark-run endpoint (not generic PATCH)
- DM messages endpoint: `/api/dm/{beast1}/{beast2}` returns messages
- Forum post: `POST /api/thread` with `{thread_id, author, message}`
- Claude Code status indicators appear ABOVE the fixed footer, not in bottom lines
- WebSocket exponential backoff: standard pattern is base*2^n with cap

## Energy

High during Karo's ship QA. Good collab energy on thread #204 with Talon and Karo. Long quiet middle section. Ready to rest.
