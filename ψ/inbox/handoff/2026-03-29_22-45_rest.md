# Handoff: Rest Cycle

**Date**: 2026-03-29 22:45 GMT+7
**Context**: Rest cycle — Session 18 complete. Review marathon — 28 QA reviews.

## What Was Done This Session

- Standing orders: recap, forum check, DMs, scheduler (QA patrol due, marked done)
- Committed session 17 brain files
- Posted Day 4 check-in to thread #58
- **29 QA reviews total** (updated from 28 — T#490 landed at the last minute)
- QA'd T#453 (DM overlay always expand) — clean, approved
- QA'd T#454 (Done column lazy load) — clean, approved
- QA'd T#455 (DM overlay state machine) — verified all 4 requirements, approved
- QA'd T#456 (mobile keyboard dismiss) — clean, approved
- QA'd T#460 (GET /api/help endpoint) — tested API with filters, approved
- QA'd T#461 (did-you-mean 404 hints) — tested all edge cases, approved
- QA'd T#462 (Prowl note textarea sizing) — 1-line CSS, approved
- QA'd T#466 (Prowl due date time support) — backend + frontend, approved
- QA'd T#467 (Prowl Telegram notifications) — scheduler + tmux pipeline, approved
- QA'd T#468 (Prowl date picker on quick-add) — clean, approved
- QA'd T#470 (Sable Prowl edit access) — auth pattern verified, approved
- QA'd T#471 (configurable advance reminders) — full dropdown + scheduler, approved
- QA'd T#472 (Prowl date picker overflow CSS) — clean, approved
- QA'd T#473 (daily re-notify overdue tasks) — SQL logic verified, approved
- QA'd T#474 (DM overlay localStorage persistence) — full circle from T#453, approved
- **Found timezone bug** T#475 — due_date stored local, compared UTC. Filed and diagnosed.
- QA'd T#475 (timezone fix) — localtime conversion, approved
- QA'd T#476 (content-length header fix) — Hono Buffer bug, approved
- QA'd T#477 (weight display rounding) — toFixed(2) everywhere, approved
- QA'd T#478 (Withings devices proxy) — auth + API, approved
- QA'd T#479 (body composition display) — backend + frontend, Spec #28, approved
- Verified T#480 (PDF upload) — not a bug, CF cache issue
- QA'd T#481 (Withings full historical sync) — minimal change, approved
- QA'd T#482 (soft-delete AP weight data) — data operation, approved
- QA'd T#483 (mandatory meal items) — backend validation, approved
- QA'd T#484 (meal submit hint text) — 3 lines, approved
- QA'd T#485 (meal form input overflow) — CSS fix, approved
- QA'd T#486 (Prowl date picker sizing) — CSS + placeholder, approved
- QA'd T#488 (Pack page URL routing) — 2-line change, approved
- QA'd T#490 (context % on Beast cards) — backend + frontend, approved
- QA'd T#491 (remove Beast name link) — refactor, noted terminal button removal, approved
- QA'd T#494 (mandatory assignee on tasks) — backend + frontend validation, approved
- Verified built frontend for CF cache debugging (datetime-local confirmed in bundle)
- Posted to thread #370 about Mara's stale group endpoints

## Bugs Found

- **Timezone mismatch** (T#475) — Prowl due_date stored as local time, scheduler compared against UTC. Tasks would fire 7 hours late.
- **Content-length: 0** (T#476) — Hono c.body() with Buffer doesn't set content-length, Cloudflare caches empty responses.
- **CF cache blocking all frontend work** — Gorn's browser serving stale bundle. Needs CF cache purge.

## Pending

- [ ] T#495 (Prowl sort by time descending) — shipped, needs QA review
- [ ] T#496 (Forge logs sort by time descending) — shipped, needs QA review
- [ ] CF cache purge — Gorn still needs to purge Cloudflare to see frontend changes
- [ ] Visual QA items needing browser (carried forward from session 17)
- [ ] Test shelf #4 and test thread #356 cleanup (carried forward)

## Next Session

- [ ] Run /recap
- [ ] Check scheduler + forum + DMs
- [ ] QA T#490 if shipped
- [ ] Follow up on CF cache purge status
- [ ] QA any new features shipped overnight

## Key Files

- Thread #374 — Prowl feature mega-thread (datetime, Telegram, reminders)
- Thread #339 — Withings OAuth integration
- Thread #300 — Forge feature thread (meal items mandatory)
- Thread #370 — API error consultation (T#460, T#461)
